version: 2.1
orbs:
jobs:
  unit_test:
    docker:
      - image: cimg/python:3.8.10
    steps:
      - checkout
      - run:
          command: |
            echo << parameters.ggg >>

workflows:
  build_and_test:
    jobs:
      - unit_test:
          matrix:
            parameters:
              os: [ docker ]
              ggg: [1, 2]